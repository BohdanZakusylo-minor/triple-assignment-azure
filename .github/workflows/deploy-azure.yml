name: Deploy to Azure Function App

on:
  push:
    branches:
      - az-deployment
  pull_request:
    branches:
      - az-deployment

env:
  AZURE_FUNCTIONAPP_NAME: ${{ secrets.AZURE_FUNCTIONAPP_NAME }}

jobs:
  deploy:
    runs-on: windows-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Setup .NET
        uses: actions/setup-dotnet@v4
        with:
          dotnet-version: "10.0.x"

      - name: Build and Publish
        run: dotnet publish ./triple-assignment-azure.csproj -c Release -o ./publish

      - name: Deploy to Azure Function
        uses: Azure/functions-action@v1.5.4
        with:
          app-name: ${{ secrets.AZURE_FUNCTIONAPP_NAME }}
          publish-profile: ${{ secrets.AZURE_FUNCTIONAPP_PUBLISH_PROFILE }}
          package: ./publish
          sku: flexconsumption
          remote-build: false
          scm-do-build-during-deployment: ""
          enable-oryx-build: ""

      #but I do not have the permission to get the client id
      # - name: Azure Login (OIDC)
      #   uses: azure/login@v2
      #   with:
      #     client-id: ${{ secrets.AZURE_CLIENT_ID }}
      #     tenant-id: ${{ secrets.AZURE_TENANT_ID }}
      #     subscription-id: ${{ secrets.AZURE_SUBSCRIPTION_ID }}

      # - name: Deploy (code-only)
      #   shell: pwsh
      #   run: |
      #     .\deploy-script.ps1 `
      #       -FunctionAppNameOverride $env:AZURE_FUNCTIONAPP_NAME

      # - name: Logout Azure
      #   if: always()
      #   run: az logout
      #   continue-on-error: true
